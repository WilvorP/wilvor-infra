[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
markers =
    unit: fast tests with no AWS or external API calls
    integration: tests against deployed development infrastructure
    infrastructure: Terraform or deployed AWS resource assertions
    smoke: live external-provider smoke tests
