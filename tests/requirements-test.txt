# Test framework
pytest>=8.0,<10
pytest-cov>=5.0,<8

# Local imports used by Lambda application modules during unit tests
boto3>=1.34,<2

# Keep the local SIGMET dependency aligned with the Lambda package.
-r ../functions/weather/sigmet/processor/requirements.txt